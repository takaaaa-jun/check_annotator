services:
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    volumes:
      # ホストの./frontendをDockerコンテナ内の/appにマウント
      - ./frontend:/app
      - /app/node_modules
